# Beispiel für SQLite (Standard):
# DATABASE_URL=sqlite+aiosqlite:///./survey_app.db

# Beispiel für PostgreSQL (wenn du es später verwendest):
# DATABASE_URL=postgresql+asyncpg://USERNAME:PASSWORD@HOSTNAME:PORT/DATABASENAME
# z.B.: DATABASE_URL=postgresql+asyncpg://survey_user:secret_password@localhost:5432/survey_db